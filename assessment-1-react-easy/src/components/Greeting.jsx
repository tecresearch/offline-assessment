import React from "react";

const Greeting=({name,age})=>{

     
//      Component Understanding: Explain what the Greeting component does and what props it accepts.
// Render Output: What will be rendered if the component is called as follows:
// <Greeting name="Alice" age={30} />
// Describe the resulting output in the browser.

// expected output: Hello, yourname!age
}

export default Greeting;